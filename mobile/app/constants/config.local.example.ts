// Example local configuration
// Rename this file to 'config.local.ts' to override default settings
// 'config.local.ts' is gitignored, so your local changes won't be committed

export const LOCAL_CONFIG = {
    // Override BASE_URL for your local development environment
    // e.g., 'http://192.168.1.5:3000'
    BASE_URL: undefined, 
};
