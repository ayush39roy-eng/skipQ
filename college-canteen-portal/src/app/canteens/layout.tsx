export const metadata = {
  title: 'Canteens',
  description: 'Browse available canteens and menus',
}

export default function CanteensLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
